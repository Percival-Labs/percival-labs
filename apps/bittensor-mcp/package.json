{
  "name": "@percival/bittensor-mcp",
  "version": "0.1.0",
  "description": "MCP server for Bittensor — metagraph queries, trust attestations, weight-copying detection",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@taostats/sdk": "^1.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.8.3"
  }
}
